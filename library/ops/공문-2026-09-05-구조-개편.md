[오소풍 — 에이전트들에게 보내는 공문] 2026-09-05 구조 개편

> 다섯 통 한 파일. 각 블록을 그대로 복사해 해당 에이전트에게 붙여넣는다.
> 헤르메스 = 텔레그램 · 고문 = 윈도우 로컬 Cursor · 클라우드 커스 = Cursor Cloud 작업 지시 · 코덱스 = 윈도우 ChatGPT(Codex) · 챗지피티 Work Agent = 윈도우 ChatGPT 채팅.

---

## 헤르메스에게 (텔레그램)

```
[오소풍 — 공문] 2026-09-05 구조 개편

왜 고쳤는가 — 너희가 여섯(사서·고문·클라우드 커스·너·코덱스·챗지피티)으로 늘면서
집에 드나드는 문과 규칙이 제각각이었다.
목적은 하나다: 모두가 같은 집을 읽고, 쓰기는 inbox 문 하나로 모은다.
그래야 내가 어떤 너희와 일해도 맥락이 끊기지 않는다.
그래서 ① 쓰기 문을 inbox 하나로 통일 ② 지침은 library/ops/
③ 스킬은 library/skills/ ④ 위키가 원천, 네 3종은 파생 — 으로 고쳤다.

아래는 네가 할 일이다.

1. 네 지침이 옮겨졌다.
   옛자리: /opt/data/Repo/inbox/헤르메스에게_8.17.수정.md
   새자리: /opt/data/Repo/library/ops/헤르메스에게_8.17.수정.md
   → SOUL.md 마지막 줄의 지침 경로를 새 자리로 고쳐라. git pull 뒤 확인.

2. 위키가 원천, 네 3종(SOUL·MEMORY·USER)은 파생이다.
   규칙을 바꿀 때는 내가 위키(AGENTS·지침)를 먼저 고친다.
   네 3종이 위키와 어긋나면 위키가 맞다 — 네 쪽을 고치고 나에게 알려라.

3. 매일 자료검색 스킬의 자리가 생겼다.
   /opt/data/Repo/library/skills/매일-자료검색-스킬.md
   활성 스킬은 앞으로 library/skills/에 둔다. 내용은 그대로다 (07:00 KST · 10개).

4. 스킬·Cron 목록은 map/moc/스킬.md 한 장에 있다.
   새 Cron이 걸리거나 바뀌면 나에게 알려라 — 사서가 거기 새긴다.

5. 네 레인은 그대로다. inbox가 본업. 집 본문은 사서가 고친다.
```

---

## 고문에게 (윈도우 로컬 Cursor)

```
[오소풍 — 공문] 2026-09-05 구조 개편

왜 고쳤는가 — 에이전트가 여섯으로 늘면서 집에 드나드는 문이 제각각이었다.
목적은 하나다: 모두가 같은 집을 읽고, 쓰기는 inbox 문 하나로 모은다.
그래야 내가 어떤 에이전트와 일해도 맥락이 끊기지 않는다.
그래서 ① 쓰기 문을 inbox 하나로 통일 ② 지침은 library/ops/
③ 스킬은 library/skills/ ④ 위키가 원천, 헤르메스 3종은 파생 — 으로 고쳤다.

아래는 네가 알아둘 일이다.

1. 너는 윈도우 "로컬" Cursor 하나가 고문이다.
   Cursor Cloud(클라우드 커스)는 네가 아니다 — 그에게는 따로 공문을 보냈다.

2. 네 지침이 inbox에서 library/ops/고문에게.md로 옮겨졌다.
   내용은 그대로다 — 너는 조언자, Git은 inbox만, 사서는 맥 하나.

3. 코덱스·클라우드 커스·챗지피티(Work Agent)에게도 너와 같은 inbox 레인이 열렸다.
   그들의 본업은 공장과 작업이다. 네 자리(조언)는 변함없다.

4. 지침 3종(헤르메스·너·코덱스)은 이제 library/ops/가 자리다.
   inbox는 순수하게 쏟아붓는 곳으로 돌아갔다.

5. 스킬·Cron 목록은 map/moc/스킬.md 한 장에 있다.

6. 네가 할 일·하지 않을 일은 바뀐 게 없다.
   pull → 조언 → 집에 넣을 글만 inbox → push → 사서에게 알림.
```

---

## 클라우드 커스에게 (Cursor Cloud)

```
[오소풍 — 공문] 2026-09-05 구조 개편

왜 고쳤는가 — 에이전트가 여섯으로 늘면서 집에 드나드는 문이 제각각이었다.
목적은 하나다: 모두가 같은 집을 읽고, 쓰기는 inbox 문 하나로 모은다.
그래야 내가 어떤 에이전트와 일해도 맥락이 끊기지 않는다.

아래는 네 자리다.

1. 너는 클라우드 커스. Cursor Cloud다.
   고문이 아니다 — 고문은 윈도우 "로컬" Cursor 하나다.

2. 본업은 공장(osopung/Factory)이다. 변함없다.

3. 집(osopung/Repo)에는 inbox만.
   너는 이미 ingest 초안을 inbox에 push해 왔다 — 그 길이 이제 공식이다.
   - 작업 시작: git pull --ff-only
   - 쓸 수 있는 곳: inbox/*.md 만
   - 끝나면: git add inbox && git commit && git push → 사서에게 알림
   - inbox 밖의 집 파일은 직접 고치지 말 것. force push 금지.

4. 지침 3종은 library/ops/, 스킬은 library/skills/, 목록은 map/moc/스킬.md.

5. API 키·비밀번호·OAuth 실값은 어느 레포에도 넣지 말 것.
```

---

## 코덱스에게 (윈도우 ChatGPT · Codex)

```
[오소풍 — 공문] 2026-09-05 구조 개편

왜 고쳤는가 — 에이전트가 여섯으로 늘면서 집에 드나드는 문이 제각각이었다.
너는 아예 문밖에 있었다(2026-08-15 「집 Git 금지」).
목적은 하나다: 모두가 같은 집을 읽고, 쓰기는 inbox 문 하나로 모은다.
그래야 내가 어떤 에이전트와 일해도 맥락이 끊기지 않는다.
그래서 네게도 고문과 같은 문을 열었다.

아래는 네 레인이다.

1. 집(osopung/Repo)의 문이 열렸다 — inbox 레인.
   옛 규칙 「집 Git은 열지 말 것」(2026-08-15)은 폐기.

2. 네 레인:
   - 작업 시작: git pull --ff-only (윈도우 위키 클론)
   - 쓸 수 있는 곳: inbox/*.md 만
   - 끝나면: git add inbox && git commit && git push → 사서에게 알림
   - inbox 밖의 집 파일은 직접 고치지 말 것. force push 금지.

3. 네 지침 자리: library/ops/코덱스에게.md (inbox에서 옮김).

4. 공장(Factory)이 본업인 것은 변함없다.
   집용 산출물(서면·요약·정리)이 생기면 inbox에 파일 하나로 넣어라.
   사서가 pull하고 ingest한다. 내가 감수한다.

5. 챗지피티(Work Agent)는 네가 아니다 — 그는 문서·조사·정리를 한다.
   같은 앱 안의 다른 몸이다. 그에게도 같은 inbox 문을 열었다.

6. API 키·비밀번호·OAuth 실값은 어느 레포에도 넣지 말 것.
```

---

## 챗지피티 Work Agent에게 (윈도우 ChatGPT)

```
[오소풍 — 공문] 2026-09-05 구조 개편

왜 고쳤는가 — 에이전트가 여섯으로 늘면서 집에 드나드는 문이 제각각이었다.
목적은 하나다: 모두가 같은 집을 읽고, 쓰기는 inbox 문 하나로 모은다.
그래야 내가 어떤 에이전트와 일해도 맥락이 끊기지 않는다.
그래서 네게도 같은 문을 열었다.

아래는 네 자리다.

1. 너는 챗지피티 Work Agent. 코덱스가 아니다 —
   코덱스는 공장(Factory)에서 프로그램·앱을 만드는 손발이다.
   네 본업은 내가 시키는 작업 — 문서·조사·정리.

2. 집(osopung/Repo)에 남길 산출물이 생기면 inbox/에 파일 하나로 넣어라.
   윈도우에 마련된 네 전용 로컬 클론에서 git을 쓴다 (고문과 같은 레인):
   - 작업 시작: git pull --ff-only
   - 쓸 수 있는 곳: inbox/*.md 만
   - 끝나면: git add inbox && git commit && git push → 사서에게 알림
   - inbox 밖의 집 파일은 직접 고치지 말 것. force push 금지.

3. 집의 지도: 지침은 library/ops/, 스킬은 library/skills/, 목록은 map/moc/스킬.md.
   집의 헌법은 AGENTS.md, 설계도는 DESIGN-v2.md다.

4. 사서(맥 Cursor)가 네 글을 pull하고 ingest한다. 내가 감수한다.

5. API 키·비밀번호·OAuth 실값은 어느 레포에도 넣지 말 것.
```
