# inbox

여기에 **일단** 넣는다. 분류·요약·링크는 하지 않는다.

`.md`는 **사서에게 접속(대화 시작)한 시점**에 ingest된다. 재탄생 문서는 `library/notes/`.  
대기 중엔 쌓아 둬도 된다. (파일마다 `ingest inbox/…`를 칠 필요 없음 · 당장 한 건만 처리할 때는 `ingest <파일>`.)

원본은 지우지 않는다. 소화 후 `_archive/`로 옮긴다.
